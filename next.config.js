/** @type {import('next').NextConfig} */
// Only needed if using Next.js instead of Create React App
// Add "use client"; as first line of App.js and rename to src/app/page.js
const nextConfig = { reactStrictMode: true };
module.exports = nextConfig;
